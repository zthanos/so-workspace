# Solution Outline Workspace (Windows)

## Prereqs (Windows)
- Docker Desktop (running)

## Build (render diagrams + export PDF)
Run from PowerShell:

```powershell
.\scripts\build_docker.ps1
```

## Output
build/pdf/Full_Doc.pdf
